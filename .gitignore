# Godot 4+ specific ignores
.godot/
/android/
.vscode/
# Third-party plugins — newcomers install these (see README "First-time setup")
addons/

# Scratch dir for Claude/session intermediate files (screenshots, logs, redirected
# stdout, etc). Keep the folder, ignore its contents.
temp/*
!temp/.gitkeep